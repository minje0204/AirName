import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#8DC7F7',
      main: '#42a5f5',
    },
    info: {
      light: '#F9F7F4',
      main: '#FFFCBF',
      dark: '#FECAF8',
    },
  },
});
export default theme;
